# evaluate-plan — STATUS: UNRESOLVED STUB

This skill folder is empty. Its intended purpose overlaps with `referee-simulator/SKILL.md`,
which the Research Evaluator already uses for plan evaluation.

**Decision required:** either delete this folder (if `referee-simulator` fully covers plan
evaluation), or write a `SKILL.md` here with a distinct, non-overlapping scope.

Until resolved, the pipeline uses `referee-simulator` for all evaluation tasks.
